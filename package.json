{
  "name": "deepmiro",
  "version": "0.8.0",
  "description": "DeepMiro — a swarm intelligence engine that rehearses the future",
  "scripts": {
    "setup:engine": "cd engine && uv sync",
    "setup:mcp": "cd mcp-server && npm ci",
    "setup:all": "npm run setup:engine && npm run setup:mcp",
    "dev": "cd engine && uv run python run.py",
    "dev:mcp": "cd mcp-server && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "AGPL-3.0"
}
